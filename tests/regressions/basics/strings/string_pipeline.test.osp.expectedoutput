=== String Pipeline ===

raw="  GET /api/users?name=alice&age=30  " len=36

isEmpty=false

contains 'GET'=true

startsWith '  '=true

endsWith '  '=true

len pipe=36  ufcs=36  direct=36

ctns pipe=true  ufcs=true  direct=true

normalized="get /api/users?name=alice&age=30"

isGetRequest=true

upper="HELLO"

trimStart="hi  "

trimEnd="  hi"

join="a-b-c"

chain="get /"

query starts at 16

substring="world"

replace="a/b/c/d"

repeat="abababab"

padS="00042"

padE="42000"

take ufcs="hel"

drop ufcs="lo"

reverse="cba"

parseInt=123

parseInt strict: rejects 'oops'

parseFloat=3.14

lines count=3

words count=2

split count=3

split empty rejected

200 ok

200 user-detail 42

200 api-fallback

201 created

404 not-found

405 method-not-allowed

=== Pipeline Complete ===

ok 1 - string pipeline preserves transformations and routing
1..1
# tests=1 passed=1 failed=0 skipped=0
