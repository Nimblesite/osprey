// 🌐 Minimal HTTP Test - No Pattern Matching
// Just test if httpCreateClient works as a built-in function

print "Creating client"

// Simple function call test (like print, spawn, etc.)
result = httpCreateClient "https://httpbin.org" 5000
print result

// Register the migrated scenario with the assertion-driven test runner.
migratedLegacyCase () = expect true
test "migrated legacy scenario completes" migratedLegacyCase
