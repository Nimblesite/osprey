// Ackermann–Péter function — deep, non-tail mutual self-recursion.
fn add(a, b) = a + b ?: 0
fn sub(a, b) = a - b ?: 0

fn ack(m, n) = match m {
    0 => add(n, 1)
    _ => match n {
        0 => ack(sub(m, 1), 1)
        _ => ack(sub(m, 1), ack(m, sub(n, 1)))
    }
}

print("${ack(3, 10)}")
