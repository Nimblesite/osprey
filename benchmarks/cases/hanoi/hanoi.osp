// Towers of Hanoi — count moves via exponential double recursion. hanoi(n)=2^n-1.
fn add(a, b) = a + b ?: 0
fn sub(a, b) = a - b ?: 0
fn hanoi(n, acc) = match n {
    0 => acc
    _ => hanoi(sub(n, 1), add(hanoi(sub(n, 1), acc), 1))
}
print("${hanoi(25, 0)}")
