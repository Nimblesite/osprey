// Factorial-style product 1*2*...*N taken mod 1000000007 (real N! overflows i64).
fn mul(a, b) = a * b ?: 0
fn mod_(a, b) = a % b ?: 0
fn step(acc, i) = mod_(mul(acc, i), 1000000007)
print("${range(1, 10000001) |> fold(1, step)}")
