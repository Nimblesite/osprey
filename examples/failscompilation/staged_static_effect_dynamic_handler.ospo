// [STAGE-HANDLE-STATIC] The other direction: a static effect must be discharged
// at compile time, so an ordinary handler for one is rejected.
static effect Tile {
    size: fn() -> int
}

fn main() = {
    handle Tile {
        size => 8
    }
    print("${perform Tile.size()}")
}
