// [EFFECTS-OP-TYPING] A performed operation must receive every declared argument.
effect ParseFailure {
    raise: fn(string) -> int
}

let recovered = {
    handle ParseFailure {
        raise message => 0
    }
    perform ParseFailure.raise()
}
