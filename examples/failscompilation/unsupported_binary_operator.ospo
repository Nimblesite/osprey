// This should fail compilation: binary operator with no right operand.
// The parser rejects the truncated expression.
// Parse-level rejection; no semantic spec section applies.

let x = 10
let y = 20  
let result = x ** y  // Power operator not supported
print(result)