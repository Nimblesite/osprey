// This should fail compilation: call to an undeclared function.
// A callee must resolve to a declaration.
// Implements [CALL-ARGUMENTS]

let result = unknownFunction(42)  // unknownFunction is not declared
print(result)