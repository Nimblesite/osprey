// This should fail compilation: constructor names an undeclared type.
// A constructor must name a type that exists.
// Implements [TYPE-UNION-REC]

type Result = Success { value: Int } | Error { message: String }

let result = NonExistingConstructor { data: 42 }  // Unknown constructor 
print(result)