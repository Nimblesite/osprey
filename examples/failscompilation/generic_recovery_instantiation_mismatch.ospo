// [EFFECTS-GENERIC-INSTANTIATION] One handled effect region has one concrete payload type.
effect Recovery<T> {
    recover: fn(T) -> T
}

let recovered = {
    handle Recovery {
        recover fallback => fallback
    }
    let number = perform Recovery.recover(7)
    let text = perform Recovery.recover("seven")
    number
}
