6:17: `control` on operation `Parallel.forEach` of a `static effect`; a static interpretation answers at compile time and has no continuation to give away, so neither `control` nor a multiplicity applies
10:4: static interpretation of `Parallel` requires all value operations; `Parallel.forEach` is a control operation
