// This should fail compilation: a match with zero arms.
// Zero arms cover no case, so the match cannot be exhaustive.
// Implements [TYPE-MATCH-EXHAUSTIVE]

type Option = Some { value: Int } | None

let option = Some { value: 42 }
let result = match option {
    // No arms provided
}
print(result)