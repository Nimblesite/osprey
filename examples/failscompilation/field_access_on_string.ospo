// This should fail compilation: field access on a string.
// The diagnostic offers `length(name)` and the UFCS `name.length()` instead.
// Implements [TYPE-FIELD-ACCESS-NON-RECORD], [BUILTIN-STRING-UFCS]

let name = "test"
let field = name.length  // Cannot access field on string
print(field)