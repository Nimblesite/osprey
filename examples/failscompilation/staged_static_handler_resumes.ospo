// [STAGE-STATIC-TAIL] A static handler is a rewrite rule, so it cannot capture
// a continuation. Capturing one is what a dynamic handler is for.
static effect Tile {
    control size: fn() -> int
}

fn main() = {
    handle static Tile {
        size => resume(8)
    }
    print("${perform Tile.size()}")
}
