// This should fail compilation: fields match no declared variant.
// A constructor's field set must match a variant of the named type.
// Implements [PATTERN-RESULT-AUTOWRAP]

type Result = Success { value: Int } | Error { message: String }

let badResult = Success { badField: 42 }  // Field doesn't match variant
print(badResult)