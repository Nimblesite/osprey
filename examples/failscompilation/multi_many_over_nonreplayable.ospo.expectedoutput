27:8: handler for `Choice.pick` may resume more than once, but the handled expression requires non-replayable effect `Email.send`; the whole handled expression's row is read, so move the non-replayable work outside the region
