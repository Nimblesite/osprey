11:21: `perform Signal<Count>` names an instantiation of dynamic effect `Signal`; a dynamic handler is keyed by effect name at runtime, so instantiations share one key and cannot be told apart (docs/plans/0024-staged-effects.md). Declare `static effect Signal`, or write `perform Signal` and let inference instantiate it
