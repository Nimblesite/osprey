// [EFFECTS-RESUME] A handler must resume with the operation's declared result type.
effect ParseFailure {
    control raise: fn(string) -> int
}

let recovered = {
    handle ParseFailure {
        raise message => resume("not an integer")
    }
    perform ParseFailure.raise("invalid number")
}
