// This should fail compilation: wrong argument count.
// A call must supply exactly the declared parameter count.
// Implements [CALL-ARGUMENTS]

fn add(x, y) = x + y

let result = add(10)  // Function expects 2 args, got 1
print(result)