// [EFFECTS-RESUME] A lambda inside an arm cannot capture its continuation.
effect E {
    control op: fn() -> int
}

fn go() -> int !E = perform E.op()

let r = {
    handle E {
        op => {
            let f = |x| => resume(x)
            f(9)
        }
    }
    go()
}
print("r=${r}")
