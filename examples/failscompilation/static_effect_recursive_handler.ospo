// [EFFECTS-STATIC-DISCHARGE] An arm executes outside its own activation, so
// performing its own operation forwards outward; with no outer handler the
// request is unhandled at program entry.
effect Loop {
    again: fn() -> int
}

let answer = {
    handle Loop {
        again => perform Loop.again()
    }
    perform Loop.again()
}
