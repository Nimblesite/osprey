// This should fail compilation: calling a non-function.
// Only an expression with a function type is callable.
// Implements [TYPE-FN-HIGHER-ORDER]

let x = 42
let result = x(10)  // Trying to call an integer as function
print(result)