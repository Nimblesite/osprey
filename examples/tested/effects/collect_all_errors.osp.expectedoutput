── signup: Nadia ──
    #1 name is empty
    #2 age is negative (-3)
    #3 score above 100 (250)
    #4 email is missing '@' (nadia_at_example.com)
    => REJECTED (4 errors collected in ONE pass)
── signup: Owen ── — clean
    => ACCEPTED (0 errors)
── signup: blank ──
    #1 name is empty
    #2 email is empty
    #3 email is missing '@' ()
    => REJECTED (3 errors collected in ONE pass)
