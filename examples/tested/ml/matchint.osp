// Default-flavor twin of matchint.ospml. Layout `match` over int literals + `_`
// lowers to the same nodes as this braced `match` — byte-identical IR.

fn label(n) = match n {
    0 => "none"
    1 => "single"
    2 => "pair"
    _ => "many"
}

print("a=${label(0)} b=${label(1)} c=${label(2)} d=${label(7)}")
0
