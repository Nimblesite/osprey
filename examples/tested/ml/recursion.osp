// Default-flavor twin of recursion.ospml. Both flavors use plain integer
// addition/subtraction and lower to byte-identical IR.

fn sumTo(n: int) -> int = match n {
    0 => 0
    _ => n + sumTo(n - 1)
}

print("sum5=${sumTo(5)} sum10=${sumTo(10)} sum1=${sumTo(1)}")
0
