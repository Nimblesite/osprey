// A recursive sum-to-n using plain integer addition/subtraction.

sumTo : int -> int
sumTo n =
    match n
        0 => 0
        _ => n + sumTo (n - 1)

print "sum5=${sumTo 5} sum10=${sumTo 10} sum1=${sumTo 1}"
0
