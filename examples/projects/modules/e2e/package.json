{
  "name": "talon-bank-e2e",
  "private": true,
  "description": "Playwright end-to-end tests for the Talon Bank Osprey modules showcase",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "install-browsers": "playwright install chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0"
  }
}
