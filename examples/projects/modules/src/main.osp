namespace app;

import billing::Greeting as Greeting
import billing::Counter::{CounterFx, run}

fn allocate() =
    perform CounterFx.next()

fn main() =
    run(action: fn() => {
        let first = allocate()
        let second = allocate()
        print("${Greeting::label(100)} counter=${first}/${second}")
    })
