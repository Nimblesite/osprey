effect Read
    value : Unit => int

relay : (Unit -> int) -> int !e
relay callback = callback ()
work : Unit -> int !Read
work () = perform Read.value ()

precomputed =
    handle static Read
        value => 41
    relay work

runtime = handler Read
    value => 42
print "${precomputed} ${runtime (\() => relay work)}"
