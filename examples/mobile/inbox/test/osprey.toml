[project]
name = "osprey-inbox-tests"
source_roots = ["../src", "../../../projects/modules/src/domain/json.ospml", "domain.test.ospml"]
default_namespace = "inboxtests"
entry = "domain.test.ospml"

[modules]
allow_wildcard_imports = false
