[project]
name = "osprey-inbox-tests"
source_roots = ["../src", "../../../projects/modules/src/domain/json.ospml", "."]
default_namespace = "inboxtests"
entry = "main.ospml"

[modules]
allow_wildcard_imports = false
