# Product screenshots

No product screenshots are included in the structural scaffold. Add captures only after the edition compiler, website release, browser, operating system, viewport, and theme are pinned in `figures.json`.

Keep untouched masters in an `assets/screenshots/masters/` directory. Crop and resize derivatives without repainting source, output, labels, or diagnostics.

